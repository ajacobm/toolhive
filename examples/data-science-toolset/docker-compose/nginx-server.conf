server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # These are optional - only enabled when services exist
    #location /penrose/ {
    #    alias /var/www/penrose/;
    #    try_files $uri $uri/ =404;
    #}

    #location /explorer/ {
    #    alias /var/www/explorer/;
    #    try_files $uri $uri/ =404;
    #}

    #location /api/penrose/ {
    #    proxy_pass http://penrose-mcp:8008/;
    #    proxy_set_header Host $host;
    #    proxy_set_header X-Real-IP $remote_addr;
    #}

    #location /api/explorer/ {
    #    proxy_pass http://data-exploration:8003/;
    #    proxy_set_header Host $host;
    #    proxy_set_header X-Real-IP $remote_addr;
    #}
}
