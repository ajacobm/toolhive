# Hugging Face API Token (optional, enables higher rate limits and private repo access)
HF_TOKEN=

# DataHub Configuration (optional, required only for metadata governance features)
DATAHUB_GMS_URL=http://datahub-gms:8080
DATAHUB_GMS_TOKEN=

# Compose profiles (default: all MCPs without backend; use with-datahub-backend for DataHub instance)
# COMPOSE_PROFILES=with-datahub-backend
